[phases.setup]
nixPkgs = ["curl", "unzip"]

[phases.install]
cmds = [
  "curl -fsSL https://deno.land/install.sh | DENO_INSTALL=/usr/local sh -s v2.9.0 && /usr/local/bin/deno ci"
]

[phases.build]
cmds = [
  "/usr/local/bin/deno task build"
]

[start]
cmd = "/usr/local/bin/deno task start"
